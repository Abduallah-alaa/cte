import 'package:flutter/material.dart';

class shema extends StatefulWidget {
  const shema({Key? key}) : super(key: key);

  @override
  State<shema> createState() => _shemaState();
}

class _shemaState extends State<shema> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
